# Render Blueprint — static VitePress docs
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: e-comm-docs
    runtime: static
    buildCommand: rm -rf node_modules && npm ci && npm run ci
    staticPublishPath: docs/.vitepress/dist
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: NPM_CONFIG_PRODUCTION
        value: "false"